{
  "name": "spider",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node --harmony ./bin/start.js",
    "test": "mocha --recursive --timeout 15000 -R spec --require should --harmony test/ --bail",
    "test-cov": "node --harmony node_modules/istanbul-harmony/lib/cli.js cover ./node_modules/mocha/bin/_mocha  -- -u exports --require should  --timeout 15000 --recursive test/ --bail",
    "test-travis": "node --harmony node_modules/istanbul-harmony/lib/cli.js cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -u exports --require should  --timeout 15000  --recursive test/ --bail"
  },
  "dependencies": {
    "cheerio": "*",
    "winston": "*",
    "rimraf": "*",
    "underscore": "*",
    "underscore.string": "*",
    "request-gb": "*",
    "istanbul-harmony": "*",
    "should": "*",
    "mocha": "*",
    "expect.js": "*"
  }
}
